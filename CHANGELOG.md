# 更新日志

本文件记录各版本的主要变更，发布 GitHub Release 时请把对应版本的中文说明复制到 Release 正文，
管理后台「系统设置 → 版本更新」会直接展示 Release 正文作为更新日志。

## v1.0.4

> ⚠️ 本次更新会让**现存的后台登录会话全部失效**，更新后需重新登录（cookie 前缀由
> `kk_` 改为 `tgg_`）。已导入 Grafana 的看板需重新导入，或手动把数据源 uid 改为
> `tgg-prometheus`。

- 新增：「OpenAI 模型」可直接从端点拉取模型列表 —— 点「获取模型」即用当前填写的
  端点与密钥请求 `/models`，结果灌入输入框候选，可点选、可输入关键词筛选，也可手填
  列表外的模型名；拉取后会直接指出当前填的模型名是否在列表中。此前模型名只能照文档
  手敲，敲错时广告判定会一直静默失败、只有日志里看得见。
- 新增：后台三个页面统一重写视觉层，浅色/深色跟随系统偏好自动切换；补齐触控目标
  尺寸与键盘焦点可见性，375px 下无横向滚动。
- 变更：系统设置页改为左侧目录分组切换，一次只显示一组 —— 九个分组的字段数相差
  17 倍（广告守卫 17 项、网络与代理 1 项），并排铺开时矮卡片下方空出约一千像素。
  目录常驻显示每组的待保存数；搜索与「仅看已修改」自动跨组显示命中项；输入框宽度
  由约 370px 增至 726px，长路径与 URL 不再被截断。
- 变更：设置页操作栏吸顶，保存按钮常驻可达；逐项脏值标记与待保存计数；校验失败时
  标注并滚动到对应字段；保存期间锁定字段，避免在途编辑被保存后的基线重建吞掉；保存
  后重拉配置以显示服务器实际存下的值（越界数值会被后端夹取），重拉失败时明确提示，
  不再谎报已生效。
- 安全：`verify.html` 不再把请求 token 注入模板上下文，消除反射型 XSS。
- 修复：admin 后台解封失败 —— 解封接口末尾一句多余的局部重绑定让 `store` 变成函数
  局部变量，前面的 `store.mark_unbanned` 因而抛 `UnboundLocalError` 一路返回 500。
  实际上 `unban_chat_member` 在崩溃前已执行成功，用户在 Telegram 侧早已解封，只是
  事件没落库，列表因此一直显示「当前封禁中」；更新后对这些记录再点一次解封即可对齐。
- 修复：新版 starlette 下后台页面 500 —— `TemplateResponse` 旧式签名已被移除，
  5 处调用迁移为新式签名（新旧环境通用）。
- 修复：验证通过公告里的用户提及与 `/ban`、`/warn` 等消息格式不一致 —— 统一为
  `tg://user` 链接 + 昵称，点击可跳转到用户页。此前有 username 时用 `@用户名`，
  对方改名或取消用户名后该提及即失效，而 user_id 是永久的。
- 修复：关键词回复编辑器规则行布局错乱 —— 新增解析模式下拉后控件数与 grid 列定义
  不匹配，删除按钮被挤到第二行、正则模式下「区分大小写」错误显示、各行下拉框无法对齐。
- 修复：未保存改动保护 —— 刷新、切群前先比对快照并确认，不再无声冲掉正在输入的内容；
  首次加载失败时禁用保存，避免把空编辑区提交成空规则文件。

## v1.0.3

> ⚠️ 消息指标口径变化：计数标签改为互斥（移除 `received`，新增 `skipped` /
> `channel_blocked`），每条消息只计一次，跨 label 求和即为真实吞吐量。
> 如你的 Grafana 面板引用了 `result="received"`，请同步调整。

- 安全：修复 admin 指令（/ban /warn /sb 等）群聊分支未校验 `allowed_chat_ids`
  白名单的问题 —— 机器人被拉进未授权群时，该群管理员可越权使用管理指令并污染
  共享数据库；现白名单非空时非授权群一律拒绝（与验证路由口径一致）。
- 安全/可靠：管理按钮回调（跳过/临时封禁/封禁）与过期清理器、Web 验证端点
  三路统一经 token 锁串行 —— 杜绝「管理员刚放行、清理器却把人封了」（或反之）
  的竞态；`/verify` 过期处理也移入锁内。
- 修复：广告误判恢复时原消息媒体永久丢失 —— 复核快照现在保存媒体 file_id，
  点「这不是广告」后图片/视频/文件等连同说明文字一并发回；file_id 失效时
  回退纯文本并明确提示媒体未恢复。
- 修复：广告守卫开关运行时重算不一致 —— 新增「意图/有效值」分离，AI 总开关、
  provider、端点、密钥任一变更后统一重算（修复关开不回升、清端点仍显示开启）；
  设置页在「已开启但实际未生效」时显示黄色警示。
- 修复：`OPENAI_ENDPOINT` 留空时守卫每条消息静默跳过、形同虚设 —— 留空现在
  默认使用官方 `https://api.openai.com`，与字段说明「可留空用官方」一致。
- 修复：低分封禁失败（机器人无权限/目标为管理员）时每条消息重复扣分、公告
  刷屏、评分无限下探 —— 现在回滚本次扣分并进入 10 分钟冷却，冷却期内静默删
  消息保持禁言效果。
- 修复：验证提示消息的 prompt_message_id 写库失败（如 SQLite 瞬时锁定）时 TTL
  自动删除被跳过 —— 现写库失败不阻断 TTL 调度，杜绝提示消息成群内永久孤儿。
- 修复：启发式规则正则 ReDoS 防护补齐 —— 切换 `regex` 引擎 + 0.5s 超时 +
  超时按 pattern 熔断禁用（与关键词模块同一标准）。
- 修复：规则热重载同秒内二次保存不生效、坏文件修复后不加载 —— 变更检测改为
  （mtime_ns, size）签名，坏文件签名变化即重试。
- 修复：修改启发式/关键词规则文件路径后不热生效（UI 显示新路径、实际用旧规则）。
- 修复：高置信快封失败后普通路径对同一用户重复调用封禁；TTL 自动删除任务改为
  落库持久化并持强引用，重启后自动恢复（到期立即补删）；消息历史字典加 LRU
  上限（10000 群）防缓慢内存泄漏；`/start verify_` 深链载荷限私聊处理。
- 变更：仪表盘 24 小时进群分布只统计真实进群事件（修复约 2 倍虚高）；低分
  封禁失败与快封失败的群公告均如实表述，不再虚报「已移出并拉黑」；
  `ADMIN_BEHIND_PROXY=true` 启动时显式告警 XFF/XFP 伪造风险。

## v1.0.2

> ⚠️ 本次更新新增 `regex` 依赖，更新完成后如未自动安装成功，
> 请手动执行 `pip install -r requirements.txt`。

- 修复：Windows 裸跑部署更新/回滚/重启后的端口竞争 —— 新增接力启动器
  （`app/relauncher.py`），等 Web 端口真正释放后才拉起新实例，避免新进程撞
  `Address already in use` 直接死掉、服务永久掉线；端口探测按 `WEB_HOST`
  实际地址族进行（`dual`/`::` 需 IPv4+IPv6 同时可绑）。
- 修复：更新/回滚后台任务一旦逃逸异常（如 git 不存在、备份损坏），状态永久
  卡在 pulling/installing，后续所有更新/回滚请求被 409 拒绝 —— 新增异常兜底，
  任务持有强引用防止被 GC 提前回收。
- 修复：用户验证成功但机器人解禁失败（权限不足/网络异常）时记录已落库为
  verified，用户被永久禁言且无法重试 —— 改为**先解禁后落库**，失败保持
  pending 可重试，并通知管理员检查机器人权限。
- 修复：重启/关停用 `os._exit` 绕过资源收尾 —— 新增关闭钩子机制，退出前
  限时执行 store/bot/redis 关闭，压缩数据丢失窗口。
- 修复：进程守护检测漏判 Docker/NSSM/Windows 任务计划程序，更新后可能新旧
  实例同时运行（抢 getUpdates、抢端口）—— 新增 `PROCESS_SUPERVISED=true`
  显式开关（NSSM/任务计划场景必须设置）、Docker/容器自动识别，并以
  `data/bot.lock` **内核级文件锁**兜底防双实例（无竞态、无 PID 复用误判）。
- 修复：入群锁字典清理误踢正在使用的锁，高入群量时可能重复发送验证提示。
- 修复：tarball（非 git）更新只增不删，新版本已移除的模块残留并可能遮蔽
  新代码 —— 更新时按旧快照差集清理废弃 `.py` 及 `__pycache__`，并补
  路径越界校验。
- 修复：广告消息删除失败（无权限/消息已不存在）时跳过了后续封禁 ——
  aiogram 3 删除失败走异常路径，现删除失败仍继续执行封禁与决策日志。
- 修复：管理员配置的关键词正则存在 ReDoS 风险（`(a+)+b` 类模式可卡死整个
  事件循环）—— 正则引擎切换为 `regex` 库（内建免疫经典灾难模式 + 原生
  超时），超时规则按 pattern 自动熔断禁用。
- 变更：Redis 评分键前缀兜底值统一为 `telegram_group_guard_bot:adscore`；
  消息指标新增 `keyword_replied` 维度（关键词回复触发量）。

## v1.0.1

- 修复：`AD_GUARD_MIN_LENGTH` 配置不生效的死配置问题，低于最小长度的消息不再送入
  LLM 广告检测，节省 API 配额并避免短消息误判。
- 修复：Prometheus 指标大面积未接线 —— `telegram_group_guard_bot_messages_total`、
  `telegram_group_guard_bot_verification_total` 现在按处理结果正常计数，评分 Redis
  降级时 `telegram_group_guard_bot_score_redis_degraded` 会正确置 1，告警规则不再形同虚设。
- 修复：Windows 部署下更新/回滚后自重启不可靠（`os.execv` 无法平滑替换进程），
  改为子进程接力重启，避免进程僵死与端口占用。
- 修复：低分提醒文案「今日评分」与永久评分的实际语义不符，已改为「累计评分」。
- 修复：验证 token 锁释放瞬间的竞态条件，避免等待者与新请求并发绕过串行化。
- 修复：更新源码包解压时的路径穿越校验不严谨（同前缀兄弟目录可能误判通过）。
- 清理：`app/bot.py` 中重复的导入与重复的初始化调用。
- 变更：Prometheus 指标前缀统一为 `telegram_group_guard_bot_`（6 个指标）；
  Redis 评分键前缀由 `kkbot:adscore` 更名为 `telegram_group_guard_bot:adscore`，
  升级后旧前缀下的历史评分不再读取、成员评分从零累计（可用 `REDIS_SCORE_PREFIX`
  环境变量改回旧前缀以保留历史数据）。
- 新增：`monitoring/` Grafana 监控仪表盘模板（13 个面板，导入你已有的 Grafana
  即用），附预览图、Prometheus 抓取配置片段与 3 条推荐告警规则，
  详见 `monitoring/README.md`。

## v1.0.0

- 新增「系统设置」页：全部配置参数可在管理后台查看与修改，保存后立即生效；
  少数需要重启的参数会明确标注，保存后可一键重启。
- 新增按群差异化配置：广告守卫开关/阈值/封禁、关键词回复与删除开关、
  消息自动删除 TTL、警告上限共 7 项可按群覆盖，未覆盖项跟随全局。
- 新增版本检查：定时与 GitHub Release 比对版本号，发现新版本时在设置页提示，
  展示中文更新日志，支持一键更新（git pull 或下载源码包）并自动重启；
  非 git 部署同样可用，data/、.env、ssl/ 会被保留。
- 新增更新前自动备份（data/ 与代码快照，保留最近 5 份）与一键回滚。
- 新增「关停服务」危险操作按钮（双重确认，防误操作）。
- 配置修改、更新/回滚/关停等管理操作会向所有授权群发送 Telegram 通知。
- 新增防误关提醒：关闭或离开管理后台页面时浏览器会弹出确认提示，
  可在页面右上角开关该提醒。
- 管理后台新增「关键词删除」规则编辑器。
